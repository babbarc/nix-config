# Per-machine env file (copy env.example to ~/.config/dotfiles/env per
# machine; never commit it - it carries machine-specific personal values)
env

# nix build output symlink in the repo root
result
